services:
  supertarot-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: supertarot-bot
    restart: unless-stopped
    env_file:
      - .env
    working_dir: /app
    command: python -m app
    volumes:
      - ./data:/app/data
    stdin_open: true
    tty: true
